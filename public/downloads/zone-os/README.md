Place the public SPICA Zone OS Windows installer here only if the file is small enough for your hosting target.

Expected filename:

- SPICA-Zone-OS-Setup.exe

For real production installers, prefer GitHub Releases or Supabase Storage and configure the URL in `lib/downloads.ts` via environment variables.
